## Kịch bản: Pronunciation — Unit 2 — /kl/, /pl/, /gr/, /pr/

**Nguồn:** `Unit_2_Pronunciation_NB_TH_VD.docx`

**Xác nhận:** Không sửa nội dung học thuật — chỉ bỏ nhãn `[NB]/[TH]/[VD]`, dòng tỉ lệ %, ghi chú
"*Phiên bản đã chữa...*", ghi chú "góc độ" (chỉ đạo nội bộ cách soạn câu), và bảng tổng hợp tỉ lệ +
toàn bộ phần chú thích (*) cuối file.

**Ngoại lệ áp dụng:** KHÔNG dùng Practice Reference Pane (giống Unit 1 — mỗi mức giới thiệu từ mới).

**Pattern "Mark the Blend" — UI đã chốt (PHẦN 1.8 `03_engine_tiengAnh.md` v1.2):** học sinh **tap
từng từ trong câu để CHỌN** (không phản hồi đúng/sai ngay lúc tap — chỉ đổi màu "đã chọn"), sau khi
chọn xong bấm **"Kiểm tra"** mới hiện đúng/sai từng từ + giải thích — KHÔNG để học sinh tự dò đáp
án bằng cách tap thử từng từ và thấy phản hồi ngay. Chọn cách này vì thao tác tay trên mobile dễ
hơn (không cần gạch chân/vẽ), và tránh hành vi "dò mù" đúng nguyên tắc chấm-khi-submit đã áp dụng
cho Drag & Drop. Câu 4 (câu bẫy, không có từ nào) — học sinh không chọn từ nào + bấm nút phụ "Không
có từ nào" trước khi bấm Kiểm tra, để phân biệt với việc quên chọn. Vì nguồn Word không giữ được
định dạng gạch chân/in đậm qua bước trích xuất, mình đã tự đánh dấu lại từ mục tiêu bằng `**từ**`
trong câu dựa theo đúng Answer key gốc (không tự thêm/bớt từ nào ngoài đáp án đã cho).

---

### PHẦN 1 — LESSON (`Unit_2_Pronunciation_lesson.html`)

**Hero:** Unit 2 · Pronunciation · /kl/, /pl/, /gr/, /pr/ — Objective: Recognise and pronounce the
consonant blends /kl/, /pl/, /gr/, and /pr/ correctly.

**Quy tắc phát âm (nguyên văn):**

> 🔊 **/kl/** — Phần sau lưỡi nâng lên chạm vòm miệng mềm cho âm /k/ → bật hơi ra → chuyển ngay
> sang /l/ (đầu lưỡi chạm nhẹ vào vòm miệng trên, ngay sau răng cửa). Không dừng giữa hai âm, phát
> liền một hơi. Ví dụ: club, clean, class — Thực hành: /k/ → /kl/ → /kliːn/
>
> 🔊 **/pl/** — Hai môi khép chặt cho âm /p/ → bật hơi ra → chuyển ngay sang /l/ (đầu lưỡi chạm nhẹ
> vào vòm miệng trên). Giữ luồng hơi thoát ra liên tục, không tách rời hai âm. Ví dụ: please,
> place, play — Thực hành: /p/ → /pl/ → /pleɪ/
>
> 🔊 **/gr/** — Phần sau lưỡi nâng lên chạm vòm miệng mềm cho âm /g/ (có rung dây thanh) → bật hơi
> ra → chuyển ngay sang /r/ (đầu lưỡi cong nhẹ lên, không chạm vòm miệng). Không dừng giữa hai âm.
> Ví dụ: green, group, ground — Thực hành: /g/ → /gr/ → /griːn/
>
> 🔊 **/pr/** — Hai môi khép chặt cho âm /p/ → bật hơi ra → chuyển ngay sang /r/ (đầu lưỡi cong nhẹ
> lên, không chạm vòm miệng). Giữ luồng hơi liên tục, không dừng giữa hai âm. Ví dụ: practise,
> present, protect — Thực hành: /p/ → /pr/ → /prækˈtɪs/

**Từ minh hoạ (4 cột, mỗi từ có nút 🔊 qua `speak()`):**

| /kl/ | /pl/ | /gr/ | /pr/ |
|---|---|---|---|
| club /klʌb/ | please /pliːz/ | green /griːn/ | practise /ˈpræk.tɪs/ |
| clean /kliːn/ | place /pleɪs/ | group /gruːp/ | present /prɪˈzent/ |
| class /klɑːs/ | play /pleɪ/ | ground /graʊnd/ | protect /prəˈtekt/ |
| climb /klaɪm/ | plan /plæn/ | grow /grəʊ/ | proud /praʊd/ |

**Read Aloud (component 9.4):** Nghe và lặp lại từng từ. Ghi âm và nộp cho AI chấm.

**Điều hướng cuối Phần 1:** `href="Unit_2_Pronunciation_practice.html"`.

---

### PHẦN 2 — PRACTICE (`Unit_2_Pronunciation_practice.html`)

**Không có Practice Reference Pane.**

#### Mức Easy

**Listen-and-Circle** (`playPromptWord()`):
1. Target: **club** — Options: A. club / B. cub / C. cup — `dap_an_dung`: A — `giai_thich_dung`: Từ gây nhiễu (cub, cup) khác âm cuối rõ ràng so với /kl/.
2. Target: **please** — Options: A. please / B. peas / C. police — `dap_an_dung`: A — `giai_thich_dung`: Từ gây nhiễu (peas, police) không có cụm phụ âm /pl/, dễ phân biệt.
3. Target: **green** — Options: A. green / B. grin / C. gain — `dap_an_dung`: A — `giai_thich_dung`: Từ gây nhiễu (grin, gain) khác nguyên âm, dễ nhận ra /gr/.
4. Target: **present** — Options: A. present / B. parent / C. pleasant — `dap_an_dung`: A — `giai_thich_dung`: Từ gây nhiễu (parent, pleasant) gần vần hơn, cần nghe kỹ cụm /pr/ ở đầu từ.
5. Target: **class** — Options: A. class / B. glass / C. grass — `dap_an_dung`: A — `giai_thich_dung`: Từ gây nhiễu (glass, grass) có cụm phụ âm gần giống (/gl/, /gr/) với /kl/, cần phân biệt tinh hơn.
6. Target: **group** — Options: A. group / B. drop / C. crop — `dap_an_dung`: A — `giai_thich_dung`: Từ gây nhiễu (drop, crop) đều có cụm phụ âm + /r/ giống /gr/ nhưng khác phụ âm đầu — bộ dễ nhầm nhất.

**Nhận diện khác biệt phát âm** (không audio):
1. Options: A. class / B. grass / C. brass *(mục tiêu /kl/)* — `dap_an_dung`: A — `giai_thich_dung`: "Grass" (/gr/) và "brass" (/br/) đều rất gần /kl/ về nhóm phụ âm + r/l, cần nghe kỹ âm đầu.
2. Options: A. play / B. pray / C. flay *(mục tiêu /pl/)* — `dap_an_dung`: A — `giai_thich_dung`: "Pray" (/pr/) và "flay" (/fl/) dễ nhầm với /pl/ vì cùng có 2 phụ âm liền + âm lướt.
3. Options: A. grow / B. glow / C. crow *(mục tiêu /gr/)* — `dap_an_dung`: A — `giai_thich_dung`: "Glow" (/gl/) và "crow" (/kr/) rất gần /gr/ về âm thanh tổng thể, đây là bộ ba dễ nhầm.
4. Options: A. proud / B. cloud / C. crowd *(mục tiêu /pr/)* — `dap_an_dung`: A — `giai_thich_dung`: "Cloud" (/kl/) và "crowd" (/kr/) cùng vần với "proud" nhưng khác phụ âm đầu — cần phân biệt tinh.

**Read Aloud — Short Phrases:** 1. a clean class / 2. please play here / 3. a green group

#### Mức Medium

**Nhận diện qua chữ viết/IPA (không audio — khác Easy vì đọc chữ thay vì nghe):**
1. Từ nào chứa /kl/? A. clean / B. green / C. brown — `dap_an_dung`: A — `giai_thich_dung`: "Green" (/gr/) và "brown" (/br/) khác hẳn /kl/ về nhóm phụ âm, dễ loại trừ qua mặt chữ.
2. Từ nào chứa /pl/? A. play / B. gray / C. tray — `dap_an_dung`: A — `giai_thich_dung`: "Gray" (/gr/) và "tray" (/tr/) đều có phụ âm + r, không phải /pl/.
3. Từ nào chứa /gr/? A. group / B. drop / C. crop — `dap_an_dung`: A — `giai_thich_dung`: "Drop" (/dr/) và "crop" (/kr/) gần vần với "group" nhưng khác phụ âm đầu.
4. Từ nào chứa /pr/? A. present / B. parent / C. absent — `dap_an_dung`: A — `giai_thich_dung`: "Parent" và "absent" không có cụm phụ âm /pr/ ở đầu, chỉ gần vần với "present".

**Listen-and-Circle:**
1. Target: **plan** — Options: A. plan / B. flan / C. clan — `dap_an_dung`: A — `giai_thich_dung`: Từ gây nhiễu "flan" (/fl/) và "clan" (/kl/ nhưng khác từ) dễ gây nhầm lẫn với /pl/.
2. Target: **grow** — Options: A. grow / B. glow / C. crow — `dap_an_dung`: A — `giai_thich_dung`: "Glow" (/gl/) và "crow" (/kr/) rất gần với /gr/ về âm thanh tổng thể.
3. Target: **proud** — Options: A. proud / B. cloud / C. crowd — `dap_an_dung`: A — `giai_thich_dung`: Ba từ "proud", "cloud", "crowd" đều có cụm phụ âm + /r/ hoặc /l/ và vần giống nhau — bộ khó phân biệt nhất.
4. Target: **present** — Options: A. present / B. peasant / C. pleasant — `dap_an_dung`: A — `giai_thich_dung`: Từ gây nhiễu "peasant" và "pleasant" gần vần với "present", cần nghe kỹ trọng âm và phụ âm đầu.
5. Target: **climb** — Options: A. climb / B. crime / C. clime — `dap_an_dung`: A — `giai_thich_dung`: "Crime" (/kr/) rất gần "climb" (/kl/) về âm thanh; "clime" là từ hiếm, kiểm tra khả năng nghe chính xác thay vì đoán theo thói quen.

**Read Aloud — Full Sentences:**
1. Our class decided to plant a green garden near the school gate.
2. She felt proud when she was asked to present her project.
3. They plan to climb the hill early to watch the sunrise.

#### Mức Difficult

**Odd One Out** (không audio, 4 lựa chọn):
1. A. club / B. clean / C. crowd / D. class — `dap_an_dung`: C (crowd = /kr/) — `giai_thich_dung`: "Crowd" (/kr/) rất gần /kl/ về vị trí cấu âm, cần nghe kỹ âm thứ 2 sau phụ âm đầu.
2. A. green / B. group / C. glow / D. grow — `dap_an_dung`: C (glow = /gl/) — `giai_thich_dung`: "Glow" (/gl/) dễ nhầm nhất với /gr/ trong nhóm vì cùng bắt đầu bằng "g".
3. A. present / B. protect / C. proud / D. plan — `dap_an_dung`: D (plan = /pl/) — `giai_thich_dung`: "Plan" (/pl/) rất gần /pr/ vì cùng bắt đầu bằng "p", chỉ khác âm thứ 2 (/l/ và /r/).

**Mark the Blend** *(pattern mới — xem ghi chú đầu file; câu 4 là câu bẫy đúng không có từ nào)*:
1. The **class** agreed to **clean** up the **playground** after school.
   `dap_an_dung`: class, clean, playground — `giai_thich_dung`: Câu ngắn có tới 3 từ chứa blend khác nhóm — cần nghe hết câu để không bỏ sót.
2. Our **group** is **planning** to **grow** vegetables in the garden this term.
   `dap_an_dung`: group, planning, grow — `giai_thich_dung`: "Planning" và "grow" đều chứa blend nhưng thuộc 2 nhóm khác nhau (/pl/ và /gr/).
3. She felt very **proud** when her project was **praised** by the whole **class**.
   `dap_an_dung`: proud, praised, class — `giai_thich_dung`: "Praised" và "class" ở hai đầu câu, cần tập trung nghe toàn bộ câu dài.
4. The children ran happily around the yard after lunch.
   `dap_an_dung`: (không có từ nào) — `giai_thich_dung`: Câu bẫy — không có từ nào chứa 4 cụm phụ âm đang học, học sinh cần tự tin xác nhận "không có" thay vì cố gán ép.
5. He **climbed** the tree quickly to **please** his younger sister.
   `dap_an_dung`: climbed, please — `giai_thich_dung`: "Climbed" và "please" nằm cách xa nhau trong câu, cần nghe toàn bộ câu.
6. Despite the heavy rain, the **group** decided to **present** their **plan** to **protect** the **ground** from flooding.
   `dap_an_dung`: group, present, plan, protect, ground — `giai_thich_dung`: Câu dài và phức tạp nhất, chứa tới 5 từ có blend thuộc cả 4 nhóm — đòi hỏi khả năng nhận diện âm xuyên suốt câu có mệnh đề nhượng bộ.

**Read Aloud — Short Passage (~62 từ):**

> Last term, our class decided to start a green project. We planned to clean up the playground and
> plant more trees near the ground close to the gate. Everyone felt proud when we finished. Our
> teacher asked us to present our project to the whole school, and we promised to protect our
> garden every single week.

**Self-assessment:**
1. I can recognise the difference between the blends /kl/, /pl/, /gr/, and /pr/ when listening.
2. I can pronounce words, phrases, sentences, or short passages containing /kl/, /pl/, /gr/, and /pr/ clearly.

---

### ⚠️ Lưu ý thiết kế (bắt buộc đọc trước khi build)
- Mobile-first, test ≤480px trước. File Practice KHÔNG hero, không lặp tiêu đề Unit. Không cần
  đồng bộ dữ liệu (không có Reference Pane).
- **Mark the Blend dùng đúng PHẦN 1.8 `03_engine_tiengAnh.md`** — tap để chọn (không phản hồi
  real-time), khoá tương tác sau khi bấm "Kiểm tra", đủ vùng chạm mỗi từ (`min-height:32px`) cho
  mobile.
- Listen-and-Circle: nút 🔊 disable khi đang phát, nghe lại không giới hạn số lần.
- Read Aloud: dùng nguyên component 9.4, `submitRecording()` để khung `// TODO`.
- Vị trí đáp án MCQ chữ phải random khi build.

### Ghi chú cho Giai đoạn 2 (Design)
- `02_design_tiengAnh.md`: 4.2 (Hero), 9.4 (Read Aloud), 4.6 (MCQ chuẩn), 4.7 (Self-assessment).
- `03_engine_tiengAnh.md` mục 4.2: `playPromptWord()` cho Listen-and-Circle; mục **1.8** cho Mark
  the Blend (`tokenizeSentence`/`toggleWord`/`checkMarkedWords` — dùng nguyên, không viết lại).
- KHÔNG dùng 4.8b (Reference Pane).
